name: Build & Release Desktop

# Triggered on tag push (v1.0.0, v1.2.3-beta.1, ...) — produces installers
# for Windows + macOS + Linux, then attaches them all to a single GitHub Release.
#
# Architecture:
#   1. Three parallel `build` jobs (one per OS) produce installers and upload
#      them as workflow artifacts. These jobs use --publish never so they don't
#      fight over the release upload.
#   2. A `release` job runs only on tag push, downloads every build artifact,
#      and attaches them to a GitHub Release in one shot using
#      softprops/action-gh-release.
#
# Quick usage:
#   git tag v1.0.1
#   git push origin v1.0.1
#
# Required repo secrets:
#   GITHUB_TOKEN              auto-provided by Actions, no setup needed.
#
# Optional macOS code-signing (recommended — without these, macOS users see
# Gatekeeper warnings and auto-update on Mac will not work):
#   APPLE_CERT_P12              base64-encoded Developer ID Application .p12
#   APPLE_CERT_PASSWORD         password for the .p12
#   APPLE_ID                    your Apple ID email
#   APPLE_APP_SPECIFIC_PASSWORD app-specific password from appleid.apple.com
#   APPLE_TEAM_ID               10-char Team ID
#
# Optional Windows code-signing:
#   WINDOWS_CERT_P12            base64-encoded Authenticode .pfx
#   WINDOWS_CERT_PASSWORD       password for the .pfx

on:
  push:
    tags:
      - "v*"
  workflow_dispatch: {}

permissions:
  contents: write   # needed by the release job to create a Release

jobs:
  build:
    name: Build (${{ matrix.label }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-14
            label: macOS
            python_target: mac-arm
          - os: windows-2022
            label: Windows
            python_target: win
          - os: ubuntu-22.04
            label: Linux
            python_target: linux
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Node 22
        uses: actions/setup-node@v5
        with:
          node-version: "22"

      - name: Install yarn
        shell: bash
        run: npm install -g yarn

      # ---- Bundled Python runtime ---------------------------------------
      - name: Download Python runtime (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        working-directory: desktop
        run: ./scripts/download-python.ps1 -Target ${{ matrix.python_target }}

      - name: Download Python runtime (Unix)
        if: runner.os != 'Windows'
        shell: bash
        working-directory: desktop
        run: bash ./scripts/download-python.sh ${{ matrix.python_target }}

      # ---- Frontend build -----------------------------------------------
      - name: Build frontend
        shell: bash
        working-directory: frontend
        env:
          REACT_APP_BACKEND_URL: http://127.0.0.1:8001
        run: |
          yarn install --frozen-lockfile
          yarn build

      - name: Install desktop deps
        shell: bash
        working-directory: desktop
        run: yarn install --frozen-lockfile

      # ---- macOS code-signing setup (only when secret is set) -----------
      - name: Import Apple certificate
        if: runner.os == 'macOS' && env.APPLE_CERT_P12 != ''
        env:
          APPLE_CERT_P12: ${{ secrets.APPLE_CERT_P12 }}
          APPLE_CERT_PASSWORD: ${{ secrets.APPLE_CERT_PASSWORD }}
        run: |
          KEYCHAIN=$RUNNER_TEMP/build.keychain
          echo "$APPLE_CERT_P12" | base64 --decode > $RUNNER_TEMP/cert.p12
          security create-keychain -p actions $KEYCHAIN
          security default-keychain -s $KEYCHAIN
          security unlock-keychain -p actions $KEYCHAIN
          security import $RUNNER_TEMP/cert.p12 -k $KEYCHAIN -P "$APPLE_CERT_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k actions $KEYCHAIN
          rm $RUNNER_TEMP/cert.p12

      # ---- Build (NO publish — uploaded as workflow artifacts below) ----
      - name: Package installer
        shell: bash
        working-directory: desktop
        env:
          # macOS notarization (only used if the secrets are set):
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          # Windows code-signing (only used if the secrets are set):
          CSC_LINK: ${{ secrets.WINDOWS_CERT_P12 }}
          CSC_KEY_PASSWORD: ${{ secrets.WINDOWS_CERT_PASSWORD }}
        run: |
          if [ "${{ runner.os }}" = "Windows" ]; then
            yarn dist:win
          elif [ "${{ runner.os }}" = "macOS" ]; then
            yarn dist:mac
          else
            yarn dist:linux
          fi

      # ---- Always upload artifacts so you can grab them from the run ----
      - name: Upload installers
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: ledgerly-${{ matrix.label }}
          path: |
            desktop/dist/*.exe
            desktop/dist/*.dmg
            desktop/dist/*.zip
            desktop/dist/*.AppImage
            desktop/dist/*.blockmap
            desktop/dist/latest*.yml
          if-no-files-found: warn
          retention-days: 14

  release:
    name: Create GitHub Release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [build]
    runs-on: ubuntu-22.04
    steps:
      - name: Download all build artifacts
        uses: actions/download-artifact@v5
        with:
          path: artifacts

      - name: List downloaded artifacts (debug)
        run: |
          echo "Contents of ./artifacts/:"
          ls -lhR artifacts/

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ github.ref_name }}
          generate_release_notes: true
          prerelease: ${{ contains(github.ref_name, '-') }}
          fail_on_unmatched_files: false
          files: |
            artifacts/**/*.exe
            artifacts/**/*.dmg
            artifacts/**/*.zip
            artifacts/**/*.AppImage
            artifacts/**/*.blockmap
            artifacts/**/latest*.yml
